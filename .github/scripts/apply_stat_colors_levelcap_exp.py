# Battle cap/status changes have been migrated into the actual source files.
# Keep this script as a no-op temporarily so existing playtest workflow builds
# do not fail while older patch workflows are retired.
print('Battle cap/status fixes are native source changes; no runtime patch needed')
